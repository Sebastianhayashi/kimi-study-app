name: Node hang diagnostic

on:
  push:
    branches:
      - design/klein-blue-company-v3
    paths:
      - .github/workflows/node-hang-diagnostic.yml

permissions:
  contents: read
  issues: write

jobs:
  isolate:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - run: npm ci
      - name: Verify Node test isolation and serial aggregate
        id: diagnostic
        shell: bash
        run: |
          set -u
          : > node-diagnostic.log
          overall=0

          echo "=== INDIVIDUAL FILES ===" | tee -a node-diagnostic.log
          for file in test/*.test.js; do
            echo "=== START $file ===" | tee -a node-diagnostic.log
            set +e
            timeout 45s node --test "$file" 2>&1 | tee -a node-diagnostic.log
            code=${PIPESTATUS[0]}
            set -e
            echo "=== END $file exit=$code ===" | tee -a node-diagnostic.log
            if [ "$code" -ne 0 ]; then
              echo "DIAGNOSTIC_FAILURE mode=individual file=$file exit=$code" | tee -a node-diagnostic.log
              overall=$code
              break
            fi
          done

          if [ "$overall" -eq 0 ]; then
            echo "=== SERIAL AGGREGATE ===" | tee -a node-diagnostic.log
            set +e
            timeout 180s node --test --test-concurrency=1 test/*.test.js 2>&1 | tee -a node-diagnostic.log
            code=${PIPESTATUS[0]}
            set -e
            echo "=== SERIAL AGGREGATE END exit=$code ===" | tee -a node-diagnostic.log
            if [ "$code" -ne 0 ]; then
              echo "DIAGNOSTIC_FAILURE mode=serial-aggregate exit=$code" | tee -a node-diagnostic.log
              overall=$code
            fi
          fi

          echo "exit_code=$overall" >> "$GITHUB_OUTPUT"
      - name: Report diagnostic result
        uses: actions/github-script@v7
        env:
          DIAGNOSTIC_EXIT: ${{ steps.diagnostic.outputs.exit_code }}
        with:
          script: |
            const fs = require('fs');
            const log = fs.readFileSync('node-diagnostic.log', 'utf8');
            const tail = log.length > 18000 ? log.slice(-18000) : log;
            const exitCode = process.env.DIAGNOSTIC_EXIT || 'unknown';
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: `[node-diagnostic] serial ${context.sha.slice(0, 7)} exit ${exitCode}`,
              body: `Temporary CI diagnostic for the hanging Node suite.\n\nCommit: \`${context.sha}\`\nExit: \`${exitCode}\`\n\n\`\`\`text\n${tail}\n\`\`\``,
            });
      - name: Fail after report
        if: steps.diagnostic.outputs.exit_code != '0'
        shell: bash
        run: exit "${{ steps.diagnostic.outputs.exit_code }}"
