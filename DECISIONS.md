# DECISIONS — Lucubro 创始人决策记录

## D-001（2026-07-27）：下一 Milestone 方向锚定 v2「文档即主页」

**决策**：R10 三版概念 demo 中，创始人选定 v2（文档即主页）为产品演进方向。产出物成为顶层组织单位；学习单元、来源、critique 挂在作品的具体缺口上。

**依据**：R10 `MILESTONE-PROPOSAL.md`（条件式推荐 M-A 真实产出闭环）+ `r10-demos/` 三版原型体验。v2 对赌 R9 盲点 2/5/6/11，对应 P0①（Real Output Loop）。

**边界（重要）**：
- 这是**方向锚定**，不是证据闭环。按 R10 纪律，v2 是 P-1/P-2 探针要检验的领先假设，v3（最小产出条）保留为保守回退。
- "如果用户选了我：证明'作品是家'比'课程是家'更符合恢复与导航心智"——创始人选择只证明创始人偏好，普通用户证据仍由 P-2 补票。
- 不触发：README/landing 叙事重写、生产代码大改、课程库下线。

**下一步**：
1. P-1 concierge 探针（创始人 0 号受试者，90 分钟脚本，见 `PROBE-PACK.md`），以 v2 形态为被测对象。
2. R11 外包轮：v2 PoL 最小实现设计，进入门 = P-1/P-2 过门。

## D-002（2026-07-27）：整包批准 R12 实施决策包（8 项 BLOCKING-R12）

**决策**：按 `OPEN-QUESTIONS-v2.md` 推荐整包批准——

- Q-01 正文默认 `local-body`，`structure-only` 为明示开关（不藏进设置）
- Q-02 内嵌正文控件 = 原生 `<textarea>` + Markdown，不做富文本
- Q-04 单 `primaryCourseId`，创建后不可自动更换
- Q-05 用户是 ready/delivered 唯一权威，模型只列未决 Gap
- Q-07 删除被 artifact 引用的课程返回 `409 COURSE_LINKED_TO_ARTIFACT`
- Q-09 新建流程先 placeholder artifact，再关联/生成课程
- Q-10 structure-only 模式每轮临时粘贴 excerpt，不落盘正文
- Q-12 PoL 数据默认禁止进研究 corpus，逐 artifact 明示授权

**效果**：R12 Flag-A（flag-off 安全壳层）解除阻塞，可开始实施。

**仍冻结（必须等 P-1/P-2/P-3 证据，创始人偏好不得替代）**：Q-03 `/app` 默认切换作品主页、Q-08 Gap 直接生成下一课、Q-11 v2/v3 最终取舍。

## D-003（2026-07-27）：材料格式支持扩展为核心旅程基础设施

**背景**：P-1 准备中发现创始人的真实材料是 .mobi，而 Lucubro 只支持 `.pdf/.epub/.txt/.md`（`lib/source-manifest.js:5-8`、`lib/onboarding.js:34-42`）。用户带着真实材料来，在第一步就被格式阻断——这是核心旅程的第一道门，不是可选功能。

**决策**：材料摄入能力扩展正式立项，范围：
- 电子书格式：.mobi、.azw3、.fb2、.cbz 等（候选实现：纯 JS 方案如 foliate-js，保持 local-first、不引入重型外部转换工具）
- 网页导入：URL → 正文提取（readability 类方案），真实任务场景（知乎问题、文章）直接可用
- 扫描版/图片型书籍（无文本层）：标记为独立的 OCR 问题，本轮不承诺（本次 P-1 用 macOS Vision OCR 一次性解决，验证了本地 OCR 可行性）

**纪律提醒**：此项与"不加一堆没用功能"不冲突——它服务核心闭环的入口；但每接一个格式必须有真实用户材料驱动，不为凑格式清单而接。

**排期**：R13 候选（在 R12 验收合并后启动，避免 server.js 改动冲突）。

## D-004（2026-07-27）：外包窗口分工——调研类开新窗口，implement 类复用固定窗口

**决策**：ChatGPT 外包轮按任务性质分流——
- **调研/探索类**（research、审计、方向对赌、demo 探索）：每次开**新对话窗口**，避免上下文污染。
- **执行/implement 类**（按 HANDOFF 合同写代码、修 finding）：复用固定窗口 `https://chatgpt.com/c/6a6612b0-4214-83e8-ba96-d1887b00ae1b`（session `gpt-bridge-lucubro-r6`），利用其积累的产品上下文。

**依据**：创始人实测 R13（审计+explore 混合型）在固定窗口内产出受既有上下文牵引，三版 demo 趋同换皮。调研需要冷启动视角，implement 需要上下文连续性。

## D-005（2026-07-27）：课程工作台方向——沉浸式全屏画布，侧栏按需弹出

**决策**：课程工作台的空间模型重定义为——
- **课节 HTML 铺平为基底**，默认全屏沉浸，无常驻左右侧栏。
- 左/右侧栏（课程结构、Tutor、笔记、工具）改为**独立弹出模块**，需要时唤出，用完即走。
- 工作台只放用户真实工作需要的工具；一切非当前任务元素默认不可见、不打扰。
- 视觉语言沿用 R13 v1「现状精修」基线（单蓝、克制、清晰层级），不引入 v2/v3 的风格实验。

**依据**：创始人实测 R13 三版 demo"只是换皮、体验更糟"——三版都保留了侧栏壳层布局，赌的是视觉风格，而真正的问题是**空间结构**（工作区被 chrome 切割，内容不是主角）。UX-AUDIT-R13 的量化证据支持此方向：工作台 27 个可见控件、课程库 30 个、笔记热图 387 个，界面客观过载。

**边界**：
- 这是对赌 v2/v3 demo 的终结——R13 T3 三版方向全部放弃，v1 仅作视觉语言基线，不作结构基线。
- 与 D-001（v2 文档即主页）不冲突：D-001 是信息架构层（产出物是顶层单位），D-005 是工作台空间层（内容铺平、chrome 隐藏）。R12 Flag-A artifact 壳层不动。
- R13-P2-03（工作台 27 控件密度）并入本方向一并解决，不再单独对赌。

**下一步**：R15 = implement 类外包轮（复用固定窗口），按本决策实现沉浸式工作台 + R13 遗留 P2 项中适配的部分。（原排期 R14，因 D-006 顺延。）

## D-006（2026-07-27）：调研冻结——R14 重定义为「消化轮」

**决策**：冻结一切新调研外包轮（deep research、case distiller、PM audit 全部暂停）。R14 不做 D-005 的沉浸式工作台，改为把已有调研结论消化进产品：

- **T1 取消按钮**：前端接通既有生成取消 API（`server.js:1295` `POST /api/courses/:id/operation/cancel` 早已存在，`public/generation-events-client.js` 从未接线）。创始人实测"生成中还不让我取消"。
- **T2 课节 rubric 入管线**：把 `RESEARCH-CORE.md` §3 的有效课节 rubric 写进首课与增量课节的生成 prompt 硬结构要求——R1 动作动词 claim（禁"了解/熟悉"）、R3 机制优先讲解、R4 来源内 worked example 标出关键步骤、R6 换表面情境 transfer、C6 完成证明=真实产物。
- **T3 确定性发布阻断**：validator 对可机检子集加 blocker（模糊动词 denylist、worked example 确定性标记）；机制深度等判断性条目只做非阻断 warning，不引入 LLM judge 作为唯一门禁（RESEARCH-CORE.md 原则）。
- **T4 试金石重生成程序**：交付可重复的本地工具，把试金石课程（`333b9b698044d3f55e05c866981dcc7a`）重置到"Mission 已确认、首课未生成"状态并走既有生成路径，不触碰其他课程。

**依据**：创始人实测 P-1 预备课首课《区分演绎与归纳》"太简单，看完根本不知道要如何行动"——对照 rubric 挂 R4/R6/C6。六轮调研的结论躺在 markdown 里没进生成管线；继续调研边际价值为负，缺口在执行。

**边界**：
- D-005 沉浸式工作台顺延至 R15，本轮不动工作台空间结构。
- 不新开任何调研窗口；R14 是 implement 类，复用固定窗口。
- 课节质量要求必须装进既有 D3 速度预算（900—1400 字、3 个 h2），是"预算内的元素规范"，不是写更长的课。

**验收试金石**：按新标准重生成的《区分演绎与归纳》，创始人看完能说清"第一步做什么"。
